{
  "name": "dsh-multi-tenant-root",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "peers:check": "pnpm peers check",
    "verify": "node scripts/verify-packages.mjs && node scripts/verify-v04.mjs",
    "probe:sqlite": "node scripts/sqlite-agent-repository-probe.mjs",
    "probe:platform": "pnpm --filter dsh-multi-tenant exec vitest run tests/dsh-mcp.integration.test.ts",
    "smoke": "node scripts/artifact-consumer-smoke.mjs --local",
    "release:preflight": "node scripts/release-preflight.mjs",
    "release:check": "pnpm verify && pnpm release:preflight && pnpm peers:check && pnpm typecheck && pnpm test && pnpm build && pnpm probe:sqlite && pnpm smoke"
  }
}
