/**
 * dsh-multi-tenant-web — DSH Web multi-tenant integration.
 *
 * SPIKE PLACEHOLDER. This package will host the tenant-bound ApiProxy facade,
 * principal binding, and per-surface authorization (unary RPC guard, mux/host
 * filtering, `/api/respond` ownership). No production surface yet — the Seam
 * Map and ADR are being produced in this milestone.
 *
 * @module dsh-multi-tenant-web
 */

export const name = 'dsh-multi-tenant-web'
