# ADR — DSH Web Multi-Tenant Enforcement Model

> **Superseded** by [`docs/web-enforcement-adr.md`](../../docs/web-enforcement-adr.md),
> which converges M2 (session genesis) + M3.0 (admission composition) with the
> web Seam Map. This file predates those, and its H1 conclusion (a core
> claim-at-create delta) is stale — M2 resolved it via the Agent `setup` hook.
