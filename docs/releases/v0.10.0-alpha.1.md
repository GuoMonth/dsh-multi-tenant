# 0.10.0-alpha.1 source candidate — not published

This file records the planned platform package version for the P2 source candidate. It is not an npm release or a claim of runtime acceptance.

The candidate replaces calibrated `allocation.profiles[]` with the fixed `cell-mvp-v1` allocation contract. The matching runtime Connector source/artifact and Cell/Operator image digests are still unbound (`source-candidate` / `null`) in [`cell-release.json`](../../packages/multi-tenant/cell-release.json). Do not use the candidate configuration with published `dsh-multi-tenant@0.9.0-alpha.1`, `@latest`, or the public runtime `0.3.0-alpha.1`; those artifacts retain the old calibration requirement.

DSH remains pinned to `0.1.5-rc.2` at `fb2c4b9e698e30edb738bca4cf0618587db7d203`. No package was published and no cluster was deployed for this source candidate.
