# 0.10.0-alpha.1 source candidate — not published

This file records the planned platform package version for the P2 source candidate. It is not an npm release. The local fixed-template combination passed [MVP internal acceptance](../evidence/cell-mvp-2026-09-22.md).

The candidate replaces calibrated `allocation.profiles[]` with the fixed `cell-mvp-v1` allocation contract. The matching runtime and Connector source are `ed914317e98a93752e8af4f7831c384fc1e92f13`; the bundled Connector SHA-256 is `e9aaa0a364cd6277ea7038025e5ffb8a8613c4eebdbe0c424c56721d162ace10`. Cell/Operator image digests and runtime release remain null in [`cell-release.json`](../../packages/multi-tenant/cell-release.json), because the candidate images are local and unpublished. Do not use the candidate configuration with published `dsh-multi-tenant@0.9.0-alpha.1`, `@latest`, or the public runtime `0.3.0-alpha.1`; those artifacts retain the old calibration requirement.

DSH remains pinned to `0.1.5-rc.2` at `fb2c4b9e698e30edb738bca4cf0618587db7d203`. No package was published. The exact local tarball/images were deployed and verified in the task cluster; artifact identities and evidence limits are in the acceptance report.
